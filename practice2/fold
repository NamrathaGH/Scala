//Package specific example
object MyClass{
    def main(args: Array[String]) {
        var elements:Seq[Int] = Seq(1,2,3,4)
        val sum = elements.fold(1)((x,y) => x+y)
        println(sum)
    }
}
